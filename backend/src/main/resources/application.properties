spring.application.name=isf-guatemala
# Server Configuration
server.port=8080
server.address=0.0.0.0

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/isf_guatemala
spring.datasource.username=isf_admin
spring.datasource.password=IsF2024@Guatemala!
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.level.org.springframework.web=INFO
logging.level.org.isf.guatemala=DEBUG

# File Upload Configuration (para fotos de odómetro)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Application Name
spring.application.name=ISF Guatemala System

# Security - Temporal para desarrollo
spring.security.user.name=admin
spring.security.user.password=admin123
